[tool:pytest]
addopts = -q --maxfail=1 --disable-warnings --cov=models --cov-report=term-missing
testpaths = tests

[coverage:run]
branch = True
source = models

[coverage:report]
show_missing = True
